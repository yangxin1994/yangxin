<%# encoding: utf-8%>
尊敬的<% if @user.blank? || @user.username.blank? %>用户<% else %> <%= @user.username %> <% end %>您好!

请在 24 小时内点击点击以下链接重置密码：

<%= @password_link %>

如果此邮件非您本人发起，请直接忽略。

—— 优数调研

（如果错误地接受到了这条信息，且没有注册优数调研账户，请直接忽略本邮件）